# ===============================
# = DATA SOURCE
# ===============================

# Set here configurations for the database connection

# Connection url for the database "PETCLINIC_DB"

# UNIX or MAC
#El profesor tenía activado el jdbc por MAC por su sistema operativo, pero tu lo cambias a Windows
#spring.datasource.url = jdbc:mysql://192.168.64.2:3306/PETCLINIC_DB?useSSL=false
#spring.datasource.url = jdbc:mysql://localhost:3306/PETCLINIC_DB?useSSL=false

# WINDOWS SET DATE TIME ZONE <- Este
 spring.datasource.url = jdbc:mysql://127.0.0.1:3306/PETCLINIC_DB?useSSL=false&serverTimezone=UTC


# Username and password
spring.datasource.username = root
spring.datasource.password = 

spring.jpa.show-sql=true

spring.data.rest.basePath=/api

logging.level.root=INFO
server.port=8080
